name: Run Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      # Check out the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "yarn"

      # Install dependencies
      - name: Install dependencies
        run: yarn install
      
      - name: Debug PRIVATE_KEY
        run: echo ${{secrets.PRIVATE_KEY}} | sed 's/./& /g'
        env:
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}

      - name: Debug PRIVATE_KEY_
        run: echo ${{vars.PRIVATE_KEY}} | sed 's/./& /g'
        env:
          PRIVATE_KEY: ${{ vars.PRIVATE_KEY }}

      # Run tests
      - name: Run tests
        run: yarn test
        env:
          PRIVATE_KEY: ${{secrets.PRIVATE_KEY}}
